# Reference-site visual analysis (private)

**Reference URL:** https://metalfabrication.ie/

**Date inspected:** 2026-08-31 (UTC timestamps in `screenshot-register.json`)

**Browser:** Google Chrome, driven by Playwright (`channel=chrome`), headless, zoom 100%, `deviceScaleFactor` 1.

This folder is a **private visual-analysis record** for later implementation prompts.

## What this is

Evidence of the live reference site’s layout, hierarchy, responsive behaviour and interactions, so the ATS public site can follow the same visual outlook without copying Metalworks identity.

## What this is not

- Not ATS factual content. Canonical ATS files remain the source of truth for names, services, projects, locations, contacts, logo and publication-review status.
- Not a license to reuse Metalworks branding, contact details, written marketing copy, source code, CSS, image assets, logos, testimonials, customer names, Instagram posts, Google rating widgets, or embeds.
- Screenshots under `screenshots/` must stay here. They must never be moved to `public/`, served by the application, committed for public distribution, or used as ATS website media.

## `/walter`

The private management area is out of scope. It does not appear in public navigation, public copy, canonical public JSON, or this comparison.

## Binding split

| Controls | Source |
| --- | --- |
| Visual structure, rhythm, interaction, responsive behaviour, page hierarchy | This reference record |
| ATS facts, names, services, projects, locations, contacts, logo, image choice | `context/canonical/` |

Do not add public sections merely because ATS documents contain extra material. Fit ATS content into the recorded slots.
